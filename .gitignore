# Per-machine / secret config — never commit these.
zsh/.zsh_local
*.local
.gitconfig.local

# Backups created by install.sh
*.bak

# OS noise
.DS_Store

# Zsh completion cache (machine/version-specific, auto-generated)
.zcompdump*
zsh/.zcompdump*
